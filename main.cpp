#include "function.h"

using namespace std;

vector<int> Tree[N];
int id;
int h[N], e[N * 2], p[N * 2];
bool check[N];



int main()
{
	int i = 0;
	sizeof(i++);
	cout << i << endl;

	//vector数组创建的树
	//tree1(Tree);
	//dfs_vec(Tree);
	//bfs_vec(Tree);
	//链式前向星创建的树
}